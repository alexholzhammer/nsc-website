# OS
.DS_Store

# Local-machine tool state (keep the shared artifacts: DESIGN.md, PRODUCT.md,
# .impeccable/design.json, .impeccable/critique/, .claude/launch.json)
.claude/settings.local.json
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/live/

# local pre-edit media backups (HEVC videos, uncropped photos) — safe to delete
_video-originals/
_image-originals/
